# Placeholder template - will be replaced in Phase 6
# This configmap confirms the Helm chart is working
apiVersion: v1
kind: ConfigMap
metadata:
  name: sre-platform-config
  namespace: application
  labels:
    app: sre-platform
    environment: {{ .Values.global.environment }}
    managed-by: argocd
data:
  environment: {{ .Values.global.environment }}
  project: {{ .Values.global.project }}
  region: {{ .Values.global.region }}
  status: "placeholder-phase5"